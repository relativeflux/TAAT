[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "taat"
version = "1.0.0"
authors = [
  {name = "Christopher Melen", email = "chris_melen@yahoo.com"}
]
dependencies = [
  "numpy==1.26.4",
  "scipy",
  "scikit-learn",
  "pandas",
  "matplotlib",
  "plotly[express]",
  "joblib",
  "soundfile",
  "librosa",
  "scikit-image",
  "tqdm",
  "mkdocs",
  "mkdocstrings[python]",
  "mkdocs-material"
]
readme = "README.md"
license = "MIT"
